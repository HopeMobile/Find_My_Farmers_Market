<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="davidhope.findmyfarmersmarket" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:enabled="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >


        <provider
            android:name="davidhope.findmyfarmersmarket.data.MarketProvider"
            android:authorities="davidhope.findmyfarmersmarket"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".SearchActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MarketResultsActivity"
            android:label="@string/title_activity_search_results"
            android:parentActivityName=".SearchActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SearchActivity" />
        </activity>

        <service
            android:name=".service.FetchMarketService"
            android:enabled="true"
            android:exported="false"
            android:label="Getting your local markets" />

        <activity
            android:name=".MarketDetail"
            android:label="@string/title_activity_market_detail"
            android:parentActivityName=".MarketResultsActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="davidhope.findmyfarmersmarket.MarketResultsActivity" />
        </activity>
    </application>

</manifest>
